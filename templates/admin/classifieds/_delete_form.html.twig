<form method="post" action="{{ path('admin_classifieds_delete', {'id': classified.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classified.id) }}">
    <button class="btn">Delete</button>
</form>
